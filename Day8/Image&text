import 'package:flutter/material.dart';

void main(){
  runApp(day9());
}

class day9 extends StatelessWidget {
  const day9({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        
        body: Container(
          decoration: BoxDecoration(
            gradient: LinearGradient(
              colors: [Color(0xff171c3D),Color(0xff010310)],
              begin: Alignment.topLeft,
              end: Alignment.bottomRight
            ),
            
          ),
          width: double.infinity,
          height: double.infinity,
          
          child:  Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Image.asset("assets/images/splash_image.png",width: 400,
              ),
              Padding(
                padding: const EdgeInsets.all(20.0),
                child: Text("Be Profi", style: TextStyle(color:Colors.white,fontSize: 30),),
              ),
              Text("Study hardly and become profi in ", style: TextStyle(color: Colors.white),),
              Text("their professional", style: TextStyle(color: Colors.white),)
            ],
          ),
          
          ),
        ),
    );
  }
}
